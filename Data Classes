data class Person(val name: String, val age: Int)

fun main() {
    val person = Person("Alice", 30)
    println(person)
    val person2 = person.copy(age = 31)  // Copy method from data class
    println(person2)
}
