fun main()
{
  val numbers = arrayOf(30, 5, 23, 11, 16)
  val sortedNumbers= numbers.sortedArray()
  println("sorted Array : ${sortedNumbers.joinToString()}")
  val reversedNumbers=numbers.reversedArray()
  println("reversed Array : ${reversedNumbers.joinToString()}")
}
